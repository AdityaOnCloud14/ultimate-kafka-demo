  spring:
    application:
      name: customer-account-state-change-services
    datasource:
      url: jdbc:postgresql://<rds-endpoint>:5432/customer_account_state_change_db
      username: ${DB_USERNAME}
      password: ${DB_PASSWORD}
    jpa:
      hibernate:
        ddl-auto: update

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}
    consumer:
      group-id: customer-account-state-change-group
      auto-offset-reset: earliest

  management:
    endpoints:
      web:
        exposure:
          include: health,info,prometheus
    metrics:
      export:
        prometheus:
          enabled: true
